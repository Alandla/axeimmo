const config = {
    colors: {
        theme: "themelight",
        main: "#17171b",
      },
      auth: {
        loginUrl: "/api/auth/signin",
        callbackUrl: "/dashboard/projects",
      }
}

export default config;